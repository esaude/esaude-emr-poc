<div class="panel panel-primary content-border-margin"><!-- demographics -->
    <div class="panel-heading">
        <h3 class="panel-title">Procurar Paciente</h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-sm-10">
                <input type="text" class="form-control input input-lg" ng-model="searchText" placeholder="NID or name...">
            </div>
            <div class="col-sm-2">
                <button type="button" class="btn btn-default btn-lg">
                    <span>
                        <img src="../../images/fingerprint_zoom.png" class="img-responsive" width="24" height="24" alt="provider">
                    </span>
                </button>
                <button type="button" class="btn btn-primary btn-lg" ng-click="linkPatientNew()">
                    <span class="glyphicon glyphicon-plus"></span>
                </button>
            </div>
        </div>
        <table st-table="patients" class="table table-striped">
            <thead>
                <tr>
                    <th>NID</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Age</th>
                    <th>Gender</th>
                    <th>Birthdate</th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="patient in patients | filter:searchText" ng-click="linkDashboard(patient)">
                    <td>{{patient.nid}}</td>
                    <td>{{patient.givenName}}</td>
                    <td>{{patient.familyName}}</td>
                    <td>{{patient.age}}</td>
                    <td>{{patient.gender}}</td>
                    <td>{{patient.birthdate}}</td>
                </tr>
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="6" class="text-center">
                        <div  st-items-by-page="10" st-pagination=""></div>
                    </td>
                </tr>
            </tfoot>
        </table>
    </div>
</div>